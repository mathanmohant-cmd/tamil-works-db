[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = [
  'rm -rf node_modules',
  'rm -rf dist',
  'npm ci --legacy-peer-deps'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npx serve dist -l 3000'
