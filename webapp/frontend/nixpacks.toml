[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = [
  'echo "Build timestamp: 1734592800"',
  'rm -rf node_modules',
  'rm -rf dist',
  'npm cache clean --force',
  'npm ci --legacy-peer-deps'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npx serve dist -l 3000'
